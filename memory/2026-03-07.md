# 2026-03-07

[PROJECT: Daily Reflection] 每日复盘
结论: 已成功完成 daily-reflection 备份与证据最小集核对，并确认今日为 cron-only 场景、无新增业务会话结论；同时识别出当前 reflection 备份白名单仍会带入 `memory/_state/`、`heartbeat-state.json` 与 archive 重命名噪音，后续需继续收窄。
文件变更: /home/re0hg/.openclaw/workspace/memory/2026-03-07.md, /home/re0hg/.openclaw/workspace/memory/lessons.md, /home/re0hg/.openclaw/workspace/memory/projects.md, /home/re0hg/.openclaw/workspace/TOOLS.md
教训: 低活动日必须用 sessions_list 交叉确认“仅 cron 会话”；当前 `git add memory` 过宽，会把状态缓存与归档移动一并带入备份噪音。
标签: #reflection #improvement #memory #cron #git

[PROJECT: Windows宿主机互操作与远控] RustDesk 安装与互操作修复闭环（07:00）
- 关键信号：WSL 调用 Windows 可执行失败的根因被确认是 `binfmt_misc` 已启用但缺失 `WSLInterop` handler（表现为 `cmd.exe/powershell.exe` 报 Exec format error、`winget.exe` 报 MZ command not found）。
- 关键信号：经 Windows 侧修复链路（`wsl --shutdown`/`wsl --update` + 服务重启）后，`/proc/sys/fs/binfmt_misc/WSLInterop` 恢复，`cmd.exe`、`powershell.exe`、`winget.exe` 在 WSL 内复测通过。
- 关键信号：RustDesk 已完成安装（1.4.1）；Windows 端服务为自动启动且运行中；用户已完成后续初始化，并明确后续 RustDesk 操作优先使用其 CLI。

[PROJECT: MLLM 每日论文速览] 发送频道偏好更新
结论: 用户明确要求后续论文默认发送到 Discord 频道 `1479351623871631361`（当前 #paper-daily）。
文件变更: /home/re0hg/.openclaw/workspace/MEMORY.md, /home/re0hg/.openclaw/workspace/memory/2026-03-07.md
标签: #paper-daily #discord #preference

[PROJECT: memory-hourly] 会话增量记忆微同步（11:00）
- 09:02 heartbeat 轮询会话中，assistant 未按 HEARTBEAT_OK 约定简答，转而输出了 MLLM 论文速览；后续需严格按 heartbeat 规则执行，避免无请求扩展回复。
- 用户再次明确论文投递偏好：后续 MLLM 每日论文发送到 Discord `1479351623871631361`（与现有长期记忆一致，无需改 MEMORY.md）。

[PROJECT: Windows宿主机浏览器调用策略] Chrome 启动方式偏好（13:08）
结论: 用户明确要求后续浏览器相关任务优先在 PowerShell 中通过 `wsl -e google-chrome-stable --proxy-server="http://127.0.0.1:7897" --remote-debugging-port=9222 --user-data-dir=$HOME/.config/google-chrome` 启动 Chrome；默认无头，只有在登录或风控/验证码场景下再有头启动并由用户接手。
文件变更: /home/re0hg/.openclaw/workspace/MEMORY.md, /home/re0hg/.openclaw/workspace/memory/2026-03-07.md
标签: #browser #chrome #wsl2 #powershell #preference

[PROJECT: 小红书自动发布链路] 固定 profile 与登录接管策略落地（15:00）
- 关键信号：用户确认手动命令可稳定弹窗，明确后续由 agent 负责启动 Chrome；仅在“需要登录/风控”时通知用户接手，不再让用户手动敲启动命令。
- 关键信号：定位到 `$HOME` 在 PowerShell/WSL 包装链路下存在展开差异；后续固定 `--user-data-dir=/home/re0hg/.config/google-chrome`，避免 profile 漂移导致登录态丢失。
- 关键信号：图文笔记《数据恢复神器》已发布成功（PUBLISH_STATUS:PUBLISHED）；第二篇“曼波配音”在无头链路触发登录态失效，需在固定 profile 下继续验证。
标签: #xiaohongshu #chrome #wsl2 #cdp #workflow

[PROJECT: Codex/OpenClaw 降智排查] CPA reasoning 参数结论固化（15:00）
- 关键信号：用户要求按 linux.do 线程做完整复核并实测，当前调用路径为 CPA `/v1/chat/completions`。
- 关键信号：修复结论明确：`/chat/completions` 必须使用 `reasoning_effort`，不要传 nested `reasoning.effort`；用户已在 CPA Payload 侧配置。
- 关键信号：按用户指定 XML 探针复测，gpt-5.3-codex / gpt-5.2-codex / gpt-5.2 输出 768，gpt-5.4 输出 512，与用户观察一致。
标签: #cpa #reasoning_effort #codex #openclaw

## 23:30 日级记忆（自动）

### 关键决策/结论
- 小红书发布链路已固化为“固定 profile + 默认无头 + 登录/风控切有头”的执行策略：统一使用 `wsl -e google-chrome-stable --proxy-server="http://127.0.0.1:7897" --remote-debugging-port=9222 --user-data-dir=/home/re0hg/.config/google-chrome`，并由 agent 负责先启动再通知用户接管登录。
- CPA 降智排查结论进一步确认：当前调用路径为 `/v1/chat/completions`，必须使用 `reasoning_effort`；`reasoning: {"effort": ...}` 属于 `/responses` 风格，不应混用。
- 按用户指定 XML 探针复测结论稳定：`gpt-5.3-codex / gpt-5.2-codex / gpt-5.2` 输出 `768`，`gpt-5.4` 输出 `512`，与线程结论和本地观测一致。
- RustDesk 安装与 WSL interop 修复闭环已完成，后续 Windows 远控相关操作优先走 RustDesk 原生 CLI。

### 重要信息/偏好
- 浏览器 profile 明确固定为绝对路径 `/home/re0hg/.config/google-chrome`，不再使用 `$HOME` 以避免 PowerShell/WSL 展开差异导致登录态漂移。
- 登录职责分工明确：用户不再手动输入启动命令；当需要登录/风控时由 agent 启动有头 Chrome 并通知用户处理。
- MLLM 每日论文速览默认发送频道保持为 Discord `1479351623871631361`。
- 用户已批准 Codex 全局规则重构方向：`AGENTS.md` 精简为目录/地图，详细规则拆分到子文件。

### 待办/后续行动
- 在固定 profile 前提下继续完成“小红书第二篇（曼波配音）”发布闭环，并复核无头链路下的登录态稳定性。
- 落地 Codex 全局 `AGENTS.md` 精简改造（目录化 + 规则拆分），完成后做一致性校验，避免遗漏硬规则。
- 维持 CPA 参数健康检查：对关键模型定期复测 `reasoning_effort` 生效性并记录基线。

### 待核对
- 固定 profile 后，`headed → headless` 切换时小红书登录态是否可长期稳定复用（当前仍有波动记录）。
- “曼波配音”图文笔记是否已完成最终发布（当前会话记录显示仍处于继续推进阶段，待最终状态回填）。
