# Weekly Memory Consolidation — 2026-W10

时间范围: 2026-03-01 ~ 2026-03-07 (Asia/Shanghai)
执行时间: 2026-03-07 00:20
lookbackDays: 7

## 本周治理动作
- 读取最近 7 天日志（2026-03-01..2026-03-07）与当前 `MEMORY.md`，完成交叉核对。
- 将可验证的滚动信息晋升为正式分类：
  - 用户偏好与约束（检索优先级、重启门禁、输出风格）
  - 关键长期配置（代理、reasoning、模型与别名）
  - 常见故障与修复策略（daily-reflection 路径不可达、cron-only 证据校验、日志先建后写）
  - 项目状态（OpenClaw 运维、多图研究、Ralph loop、小红书运营）
- 清理 `MEMORY.md` 中“近期重要更新（滚动7天）”为仅保留待核对事项，避免重复堆积。

## 本周新增/确认的关键记忆
1. 用户长期配置偏好已稳定：Discord 代理 `127.0.0.1:7897`，默认 reasoning=off，默认模型 `gpt-5.4`，别名 `complex=gpt-5.2-codex`、`medium=gpt-5.4`。
2. 运维硬规则已确认：凡修改配置文件，重启前必须先执行 `openclaw doctor --non-interactive` 且无阻塞错误。
3. 研究任务执行策略已确认：中长周期研究先结构化 PRD，再检索与精读；长任务优先 subagent，单次超时 2 小时。
4. Daily Reflection 风险画像稳定：路径不可达场景走“备份失败不阻断”，并在低活动日以 `sessions_list` 交叉核对。

## 待核对项（延续）
- `dangerouslyDisableDeviceAuth=true` 风险回退方案与验证窗口。
- WSL→Windows interop 修复是否生效（待用户复测）。

## 统计
- 扫描日志文件: 7
- 交叉核对索引文件: 3（`projects.md`/`lessons.md`/`MEMORY.md`）
- 晋升到正式分类条目: 4 组
- 保留待核对条目: 2
